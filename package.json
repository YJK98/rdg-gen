{
  "name": "data-record",
  "version": "1.0.0",
  "description": "random reading generator for data recording sheet",
  "author": "yjk98",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon server/index.js --watch server",
    "build": "nuxt build",
    "start": "cross-env NODE_ENV=production node server/index.js",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "express": "^4.15.3",
    "nuxt": "^1.0.0",
    "nuxt-clipboard": "^0.0.7",
    "random": "^3.0.6",
    "vuetify": "^1.0.19"
  },
  "devDependencies": {
    "cross-env": "^5.0.1",
    "nodemon": "^1.11.0",
    "stylus": "^0.54.5",
    "stylus-loader": "^3.0.1"
  }
}
